# Ansich v1 分阶段实施计划

本目录把 [Ansich v0.2 设计](../ansich-design-document.md) 拆成十二个可独立执行、可独立验收的阶段。阶段按顺序交付，每个阶段都必须形成一条可运行的纵向切片，而不是只提交数据表、只提交探针或只提交页面。

## 当前实施状态（2026-07-20）

- Phase 1 已形成可运行纵向切片：嵌入式启动、Run→Task 生命周期、严格 Observation 契约、有界 fail-open Collector、Observation/job 原子写入、独立 leased projector、Task/Scope/Belief 投影、重放、管理员 API，以及 dev/op 工作区列表和详情页均已实现。
- SQLite、Gateway、Run worker、API、前端类型/lint、前端全量单测，以及管理员列表→Task 详情和 503 的真实浏览器 E2E 已经验证；Phase 1 尚未标记最终完成，因为 PostgreSQL 集成矩阵、关闭 Ansich 的基准对比和生产环境 paper drill 仍待执行。
- Phase 2 已形成可运行的本地纵向切片：逻辑 Step 与 adapter attempt 分层、内部 LLM system operation、有序模型输入快照、payload 分层、SQL 投影/API，以及 Operations 的 Timeline/Steps/Context 页面均已实现。SQLite、相关后端回归、前端全量单测和真实浏览器 E2E 已验证；PostgreSQL 迁移矩阵、关闭 Ansich 的性能基准和生产 paper drill 完成前不标记最终完成。
- Phase 3 已形成可运行的本地纵向切片：Tool intent、raw callable boundary、模型可见结果和 unknown/denied authorization attachment point 分离；Task terminal reconciliation、重启恢复、typed SQL 投影、issued/executed usage、管理员 API 以及四段责任链页面均已实现。SQLite migration/rebuild、后端 Ansich 回归、前端类型/lint 和 raw/visible 权限边界已验证；PostgreSQL 迁移矩阵与生产负载演练仍属于后续生产就绪门禁。
- Phase 4 已形成可运行的本地纵向切片：typed producer/derivation 表、纯 BFS 谱系遍历、压缩前冻结的 source/preserved/removed inventory、summary block 与 `compressed` 边、snapshot 反向 membership、metadata-only 的 lineage/exposure/snapshot/compression API 以及 Context & Lineage 页面均已实现（详见 [04-context-lineage-and-compression.md](04-context-lineage-and-compression.md) 的实现状态）。PostgreSQL 迁移矩阵、关闭 Ansich 的基准对比和生产 paper drill 完成前不标记最终完成。
- Phase 5 已形成可运行的本地纵向切片：owner-only Outer Run heartbeat、完整 rule Belief、local Usage、实际 runtime Budget 快照、`0016_ansich_operations` 投影/重放、active-task read model、管理员 API 与自适应轮询 Operator Lens 已落地（详见 [05-active-tasks-heartbeats-and-budgets.md](05-active-tasks-heartbeats-and-budgets.md)）。SQLite migration/rebuild、PostgreSQL DDL 语义、后端与前端回归已覆盖；真实 PostgreSQL 升级矩阵、关闭 Ansich 的基准和生产 paper drill 仍是最终生产就绪门禁。
- Phase 7 已形成本地完整闭环：lead/subagent 实际 assembly 产出 runtime descriptor，Task admission 解析并绑定不可变 AgentRelease，typed projection/API 支持 release 查询与结构化比较，provider-reported model 作为 attempt evidence 驱动 `configuration_drift` Belief/Alert，Task 详情可查看脱敏配置、组件哈希和 release diff（详见 [07-agent-release-and-comparison.md](07-agent-release-and-comparison.md)）。SQLite 迁移、PostgreSQL DDL 编译、后端 Ansich 回归、前端类型/单元检查与关键 Playwright 流程已覆盖；真实 PostgreSQL 升级矩阵、关闭 Ansich 的基准和生产 paper drill 仍是最终生产就绪门禁。
- Phase 8 已形成本地纵向切片：实际 subagent 委派产生独立 child Task/context/release/probes/heartbeat，typed spawn 与 ancestry closure 支持多层树和环/第二 parent 防御，source-aware contribution 投影提供 local/inclusive Usage、late relation backfill 与可解释 source breakdown；tree/children/scoped usage/root-only API 及 Task tree、Budget scope、lineage child link UI 已落地（详见 [08-subagent-task-tree-and-inclusive-usage.md](08-subagent-task-tree-and-inclusive-usage.md)）。migration `0019_ansich_task_tree_usage` 覆盖历史 self usage 转换与 read-model 重建；真实 PostgreSQL 升级矩阵、关闭 Ansich 的基准和生产 paper drill 仍是最终生产就绪门禁。
- Phase 9 已形成本地纵向切片：多 Scope/role/evidence、不可变 AuthorizationSnapshot、potential/intended/observed Effect、`scope-safety@1` conclusion/Alert、`0020_ansich_scope_safety` typed SQL 投影与 replay、管理员 API 及 “Scopes & effects” UI 已落地（详见 [09-scope-authorization-and-effects.md](09-scope-authorization-and-effects.md)）。unknown coverage 保持显式，Ansich probe fail-open 且不改变 DeerFlow 授权决定；真实 PostgreSQL 升级矩阵、关闭 Ansich 的基准和生产 paper drill 仍是最终门禁。
- Phase 10 已形成本地纵向切片：`evaluation.recorded` v1 契约与唯一 core validator（verdict/score+scale、hard fidelity 只对带 suite/case 的 benchmark/unit test 开放、`earliest_erroneous_step` 必须指名本 Task 的 Step）、升级到 `ansich-default@2.0.0` 的 Belief resolver（新增 `soft_human` 档，v1 逐字保留可按版本重放）、migration `0023_ansich_evaluations` 的 `ansich_evaluation_index` 与 `ansich_release_quality_stats`、`evaluation-projector@1`（索引行 + `quality.<dimension>` assertion + 按格加锁重算的 release 聚合）、五个具名维度的 unassessed 合成、record/read 服务面与 cohort 可比性规则，以及 Gateway 的评估录入/读取/release quality/审计 no-store payload 端点、compare 的 quality 区块、feedback→evaluation 的 fail-open 桥接和 Task 详情第五个入口 `?view=evaluations` 与 release 比较 Quality 卡片均已落地（详见 [10-evaluation-and-semantic-beliefs.md](10-evaluation-and-semantic-beliefs.md) 的实现状态）。本地验收覆盖 SQLite 迁移/重放、后端 Ansich 回归（430 项）与前端 lint/typecheck 及单元测试（804 项）；真实 PostgreSQL 升级矩阵已由 Phase 11 前的加固批执行（见下条），关闭 Ansich 的性能基准和生产 paper drill 仍是最终生产就绪门禁。Phase 11 的第一批（11-A，采集与写入韧性）已落地，见下方条目；Phase 11 的 B/C 批与 Phase 12 尚未开始实现。
- **Phase 11 前的加固批（2026-08-19，`d463a604`..`093281af`，含批终审后的修复波）**：不是新阶段，而是把「归属 Phase 11 前」的到期跟进项一次性结清，并首次让迁移链跑在真实 PostgreSQL 上。结清的项：phase-8 M1/M2（`_refresh_usage_summary` 成为 wall_time summary 的唯一写者；wall_time 改为每 `(aggregate, source)` 一行的 max 型高水位通道，写入前先加锁再读，历史 per-tick 行由迁移 `0024_ansich_wall_time_watermarks` 折叠）、phase-9 M1/M2（bash 首命令词在单一简单命令下产出 `filesystem_delete`/`permission_change`，但 observed 侧只记 `inferred` fidelity 并保留 `unknown` 伴随行，因此**不**改变 `unverified_effect` 结论；scope-safety 改为按 watermark 区间增量重估，区间下界落在持久表 `ansich_assessor_watermarks`，迁移 `0025_ansich_assessor_watermarks`）、phase-6 L2（孤儿 `requested` operator action 的 5 分钟保守过期 + CAS 接管，审计终态留在不可变 Observation 流里）、F10-8（externalized payload 的 scope/authorization/effect 三个 `_validate_subject` 分支补 None 守卫）、F10-10（settle 时序门禁 `only_test_driven_assessments`，**充分性只被 Task 8 的验收负载证明过**，更重负载下仍有 2 条已上门禁的测试翻红，边界已入档），以及 `GET /tasks/{id}/usage?by=model`——回溯验证矩阵里唯一那笔弱通过（#3645 缺 by-model 聚合）就此结清。本批同时在 phase-10-review-followups.md 新登记了五条常驻发现（F10-19 late spawn 与 sum 型 contribution 的永久丢失窗口、F10-20 `_refresh_usage_summary` 未串行化、F10-21 生产 effect 恒 `scope_id=None` 致两类越权结论不可达、F10-22 `sudo`/`env` 包装命令的分类未裁定、F10-23 assessor 读证据不 hydrate externalized payload——毒行会停摆整个 Task 的 scope-safety），均归 Phase 11。批终审（结论 With fixes，零批级正确性缺陷）之后的修复波 `ac472261`/`093281af` 补齐了记录与覆盖：F10-10 新增假说 (c)（assessor 水位在依赖延迟自愈路径上回退，正是 `[0,2,3,1,1]` 证据的形状，候选修法归 Phase 11）、phase-9 M2 与 backend/AGENTS.md 的「已收敛结论不再重写」加上回退例外限定、PG 冒烟补齐本批两条 PG 敏感构造（wall_time 高水位 upsert 与 assessor 水位写入首次在真 PG 上执行）、复现负载落盘为 `backend/tests/support/ansich_contention_repro.sh`、`_claim_projection_job` 预校验补上回归钉子。
- **环境观测切片（2026-08-19，Phase 10 之后、Phase 11 之前，不占用 Phase 编号）**：把沙箱执行环境的 OS 级信号（fd、io、内存、磁盘、PSI）作为第二类证据来源接入 Ansich。新增 `environment.sampled` Observation（subject 为 sandbox/host Scope）与 `ScopeKind` 新增值 `host`；两条采集路径——`AnsichEnvironmentProbe`（仿 `AnsichTaskHeartbeat`，按 provider 分派：AIO 容器 cgroup、local 宿主磁盘/PSI 并声明 host+sandbox 两个 Scope、其余 provider 只发一条 `uninstrumented` 声明）与 local bash 按命令 `ProcessGroupSampler`（ansich 无关，经 `asyncio.to_thread` 边界的显式 `Context` 回传，由既有 Ansich tool probe 链发射）；`environment-projector@1` 注册在 `task-safety` 之后，产出三张读模型（migration `0026_ansich_environment`）；`environment-pressure@1` 挂进现有周期 operations 评估循环，产出 `environment_pressure:<metric>`/`environment_leak:fd_open` 两类 transition-only Assertion 与两个新 `AlertType`，Alert 读模型行附 `possibly_affected_task_ids`（非空覆盖语义；时间相关、非因果），evidence 仍是贡献样本的 obs 引用；`GET /tasks/{task_id}/environment` 与 ToolCall 详情的 additive `environment_sample` 字段是读侧接口，前端“运行环境”面板落地。per_command 数据严格不产生 Alert（读侧展示专用），泄漏规则只吃 `container` + `continuous` 输入，缺数据永远是 unknown。详见 [environment-observability.md](environment-observability.md)；与 Phase 11 的 scope-subject 映射和 retention 矩阵的关系见该文件 §2。
- **Phase 11-A 采集与写入韧性（2026-08-20，`246964e5`..`31dc9907`，含批内基线卫生修复 HOTFIX-0 `ae731b18`）**：Phase 11 拆成三批交付，这是第一批，范围严格是 [11-resilience-replay-and-retention.md](11-resilience-replay-and-retention.md) 的 **§2（Collector 与 BatchWriter 加固）加上 F10-7**；§3/§4/§9 归 B 批，§5–§8 归 C 批，本批一条都没碰。零迁移，head 仍是 `0026_ansich_environment`。落地的语义：
  - **生命周期**：`derive_status` 是对当前事实的**纯函数**（无记忆、不保存状态，因此不会与它所描述的服务失步），状态从四个扩到七个（新增 `starting`/`recovering`/`shutting_down`）。钳制集 `LEGAL_TRANSITIONS` 比 §2 的示意图**更宽**：§2 的八条边是标称弧，作为**被钉住的子集**保留（`backend/tests/ansich/test_lifecycle.py::SPEC_SECTION_2_EDGES`），钳制集则是这套推导真正可达的闭包（17 条边，每条都在模块里写了一行可达性理由，覆盖运维恢复、任意运行态下的 stop、start 之后的第一次读，以及重启）。**把钳制集放宽到闭包是一处显式记录的 spec 偏离**——照着示意图钳制会在正常运行中翻红；必须保持非法的那一半由否定式序列钉住。`recovering` 必须有**事故证据**（未上报的丢失范围，或 writer 的重试积压），**绝不**由裸队列深度推出：一次负载高峰不是事故，拿它当证据会凭空造出 §2 图里没有的 `healthy -> recovering`。另加 start/stop 的重入守卫。
  - **per-producer 账目**：每个 `(producer, instance)` 一行 accepted/dropped/last-seq/序列化失败/最后一次成功 flush，LRU 上界 256。措辞纪律：`evicted_producer_count` 数的是**驱逐事件**，不是被驱逐的 producer 数。
  - **writer**：被拒的批次先做有界指数退避重试（100ms 起翻倍、上限 5s、至多 5 次），**等待期间不持任何锁**（`persist_lock` 只覆盖出队加第一次尝试；因此 terminal barrier 可以越过一个**正在等待**的 writer，但绝不会越过一个**正在写**的 writer——ingest 序与 collector 序分叉是本系统既有的晚到形状，watermark 加宽正是为它存在的）。批次预算耗尽后逐条隔离（`writer_item_max_attempts`），一条毒行只赔它自己，reason 为 `poison_observation`；`poison_observation_count` 分不清「真毒行」与「拖过全部预算的总体故障」。**一次总体故障拖过全部预算时，队列会以有界速率排空成丢失**——这是 v1 没有 spool/WAL 的既定代价，进程崩溃前未 flush 的数据仍是明确的 known limitation。另一处诚实的不对称：提交落库之后才到达的取消会把一个 durable 批次整批记成丢失（幻影丢失），方向是**宁可多报**、且是进程本地的。
  - **terminal barrier**：以 **collector sequence** 为界（按序排空所有 ≤ S 的排队项），**超时不再丢弃**——没能落库的行退回**队列头**，由 writer 在存储恢复后写入（与旧的丢弃语义相反）；barrier 自己取走的选择集会登记进 in-flight，因此 `in_flight_count` 的含义是**全部在途行**，包含 barrier 的写。`FlushResult` 新增 `{persisted_through, lost_ranges, timed_out}`。措辞纪律：`persisted_through` 是**连续**已持久化的最高 collector 序号，因此**任何一次丢失之后它就退化为首次事故前的前缀**并停在那里——这是刻意选的语义（宁可少报，不可多报），必须与 `lost_ranges` 配对读才是完整图景。
  - **进程级丢失范围**：`observability.degraded` 以丢失范围的 Task 为 subject，所以 `task_id=None` 的进程级范围**根本写不出去**。它们被放进一个显式的未上报桶，以 health 字段 `unreported_global_lost_range_count` 呈现，并且**只要没被写出去就绝不标记为已上报**。**进程级范围的账目已经诚实，持久化留待 B 批的 host-`Scope` subject 设计**。
  - **回执（F10-7 结清）**：改为按 observation 所处**状态**解析的四级阶梯（在队列里或在 writer 手上 → `pending`；已被记为丢失 → `failed`；已落库 → 由它自己的 projection job 决定，而 accepted 却无 job 读作 `failed`；三处都不在 → `failed`，推定丢失）。连带修正了反向的一半：barrier 超时现在读作 `pending`，因为那一行还活着。末档「三处都不在即丢失」的前提是 **id 归本进程所有**（单 collector），已写进 docstring。
  - **停机**：`stop_drain_timeout_ms` 通过取消来界定**这次尝试本身**（一个永不返回的 persist 会被取消），而不只是界定排队量或重试次数，所以卡死的存储调用**扛不住** shutdown——但这只对 **writer** 成立，projector join 仍是无界的（§8 的完整关闭顺序归 C 批）。预算耗尽时余下的行被记为丢失并发一条不受限速的 WARNING；**预算内完成的排空即使记了丢失也只体现在计数器上、不发任何警告**，这是一处运维可见性缺口，读「安静的停机」时必须知道。
  - **丢失上报**：coalescing 不再静默延长一条已上报的范围（延长部分自己开一条、自己被上报）；合成的 `ansich-collector` 上报者仍然把 collector 序号写进 `producer_seq`，这是最后一处混用两个编号空间的地方。
  - **配置**：新增五个 startup-only knob（`writer_retry_max_attempts` 5、`writer_backoff_initial_ms` 100、`writer_backoff_max_ms` 5000、`writer_item_max_attempts` 2、`stop_drain_timeout_ms` 10000），已同步 `config.example.yaml`（`config_version` 33→34）；本批同时把整个 `ansich` 段的字段描述统一补上 `startup-only:` 前缀（纯描述，零行为）。前端七态端到端支持，`starting`/`shutting_down` 渲染为**静默的阶段行**而不是绿色的「数据健康」。
  - **HOTFIX-0**（`ae731b18`）：`budget_health` 的两个生产写者共用同一个纯函数 `order_wall_time_evidence` 收敛证据序（时钟无关性已用 2026/2099 双 fixture 证明）；该危害的**断言结构形状**那一半登记为 F10-24，归 B 批。
  - **本批新登记的常驻发现**：F10-24（上述）、F10-25（`record_evaluation` 的重放查询无守卫，存储不可用时异常直接抛给调用方，RA6 回执阶梯整条不可达；既存缺陷）、F10-26（rebuild 完整性：`rebuild_projections()` 可能在依赖延迟的 job 尚未结算时就宣告完成，本批四次目击）、F10-27（装配不对称，外加 `operations_assessment_interval_ms` 至今没有配置字段、生产恒为 1000ms），均登记在 [phase-10-review-followups.md](phase-10-review-followups.md)；F10-10 的留观记录补了「门禁覆盖的是评估节奏、不覆盖依赖等待那次读」这条确切边界，以及本批两次假说 (c) 目击（一次文本完整，一次因 `tail` 管道截断而**不构成完整证据**）。
  - **B/C 批仍欠**：多 worker 的 lease/watermark/health 合并与 Alert 主题映射（§3/§4/§9）全部未动；replay、retention、raw payload 读审计与关闭顺序（§5–§8）全部未动。进程级丢失范围的持久化是 B 批的前置，projector join 的时限是 C 批的前置。
- **Phase 11-B 多 worker 投影韧性与健康面（2026-08-20..22，`cc80a262`..`e275372c` 加本次文档提交）**：Phase 11 的第二批，范围是 [11-resilience-replay-and-retention.md](11-resilience-replay-and-retention.md) 的 **§3/§4/§9**，外加把并发债一次结清；§2 已由 A 批落地，§5–§8 仍归 C 批，本批一条都没碰。**一个迁移 `0027_ansich_lease_generation`**（两张作业表各加一列 `lease_generation`，加一个 `(projector_name, status)` 索引；无新表），head 自此是 `0027`。落地的语义：
  - **lease 的 compare-and-set**：认领在同事务里把 `lease_generation` 加一并记住它，此后该作业的每一次写（完成、出错重臂、吸收）都带 `WHERE job_id = :id AND lease_generation = :claimed`；rowcount 为 0 意味着这行已被接管，写**静默放弃**、绝不抛（新 owner 负责结果，抛异常只会把行在它脚下重臂）。为什么用代际而不是 owner：`lease_owner` 是每进程一个 `uuid4`，租约过期后同一个 worker 重认领会读回自己的 id，这是 owner-only CAS 看不见的 ABA。投影侧靠幂等兜底；**assessor 侧不行**——评估还推进 `ansich_assessor_watermarks`，那是「已判过什么」的声明，幂等修不了它，所以被丢弃的完成让**整条评估事务回滚**（裁决 PB4：断言、告警 episode、水位与作业状态同生共死）。
  - **新作业状态 `retry`**：硬错误重臂到 `retry`（attempt 已花掉），可重放的依赖等待仍留在 `pending`（它把 attempt 还了回去，从来不是一次重试）；两个认领谓词与 `has_pending_for_task` 都接受它，`processing` 刻意不改名，回执把每个仍可认领的状态照旧读作 `pending`。
  - **rebuild/retry 是单操作者维护操作**：PG 上各自全程持一把 `pg_advisory_lock`（与 bootstrap 的 key 刻意不同），第二个操作者**阻塞**而不是被拒；锁挂在**一条钉住的 `AsyncConnection`** 上而不是 `Session` 上——T9 的双 worker tier 抓到的生产缺陷就是后者：`Session.rollback()` 把连接还回池，解锁可能落到**另一个** backend，真锁一直被持有，所有 worker 的维护操作排队到 `command_timeout`。`finally` 先回滚再解锁（DBAPI 出错后连接是 inactive 的，先解锁会被客户端吞掉），`rebuild` 的 re-pend 靠**抬代际**而不是靠锁挡住先于它认领的在飞 worker。
  - **`rebuild_projections()` 返回 `RebuildOutcome(replayed, unsettled)`**（F10-26）：「这一轮没认领到」不等于「重建完成了」——依赖延迟的作业在 250ms 退避里、认领也可能被接管。等待被否决（它同时持维护锁与调用方的 `_projection_lock`），显式报告加「想要完整性就再调一次」是选定的语义。
  - **host `Scope` 主体与 `observability.lost`**：SQL 后端的 `start()` 以固定 `source_event_id`（`ansich:host-scope:{hostname}`）记一条 `scope.snapshotted`，producer 去重使它跨重启、跨 worker 幂等，`ansich.safety.host_scope_id()` 让它与环境探针的 host 声明收敛到同一个实体。这类记录的 `task_id` 是文档化的哨兵 `ANSICH_BOOTSTRAP_TASK_ID`（合法仅因为 `ansich_observations.task_id` 没有外键），而每一台**有**外键的 Task 级机器都必须躲开它——`_assessors_for_observation` 对哨兵行拒绝整个 assessor 家族，否则失败的插入会把它搭车的那次投影永久打死。A 批那个「写不出去的进程级丢失桶」自此有了主体：新 kind `observability.lost` v1（不碰冻结的 `observability.degraded`），**行落库成功才出桶**。
  - **两个 process-subject Alert 生产者**：`projection_failure` 与 `observability_degradation` 活在周期 `assess_operations` 里、与 `_assess_environment` 并排，规则是 `packages/ansich/ansich/process_health.py` 里的纯函数，一次调用携带该 `(subject, rule, alert_type)` 下的**完整键集**。两者都遵守**句柄规则**：用纯函数算 id + 自己查实体在不在，**绝不**用 `AnsichService.host_scope_id`（那句话的意思只是「本进程铸造成功了」）。边界写死：`projection_failure` 只数 `failed`（`retry` 是在飞的工作）且只覆盖**投影**作业（证据链是作业自带的 `obs_id`，assessor 作业没有这一列——因此一条 durably failed 的 assessor 作业**不产 Alert**，登记在 F10-18）；`observability_degradation` 是**近期性**判断（900s 窗口），因为那条流只增不减，「曾经丢过吗」是一个永远无法 resolve 的问题。丢失扫描的 500 行上界的真实代价是**静默的永不告警**（不是提前恢复），所以它不修而是**让漏报可被发现**：一条不设上界的 `COUNT` 并排跑，超出即给该轮每条断言打上 `scan_truncated` 并发一条限速 WARNING。
  - **健康面合并数据库真相**（§4/§9）：`GET /api/ansich/health` 现在答**两个不同分量的数字**。`get_health()` 一字未动（同步、与 `record_batch` 共享一把锁、零 IO——DB 往返进它就是把存储延迟直接压上采集热路）；数据库那半是独立的 `async get_database_health()`，自带 `health_database_timeout_ms`（2000）与兜底，任何失败都答 `database.status="unreachable"` 且块内每个数字为 `None`（**`None` 是未知，不是零**），进程侧照常全量返回。只有 `database.failed_jobs` 是**权威**的；同名的进程字段保名并明确为 **advisory**。每个投影器带 `(pending, retry, processing, failed)` 与 `complete_through`——后者是**连续性**标记（未结算作业的 `min(ingest_seq) - 1`），不是最大值：一条 durably failed 的作业把它按住，这正是它可以被读作「这条线以下没有欠账」的原因。**读的边界只有 `status IN (未结算)` 这个谓词**：实测 1.2M 行时无谓词是 ~200ms 的并行顺扫（1 Hz 的 ops tick 与每次 `GET /health` 都跑），有谓词是 ~0.1ms 的索引扫，而且服务它的是**状态在前**的 `..._claim` 索引——`0027` 那个 projector-first 索引服务的是另外两个读（投影失败告警的取证查询、spawn-usage 对账的在飞闸门）。快照内的读**顺序**是正确性属性：`MAX(ingest_seq)` 必须先读，否则连续性标记会**多报**，而多报会让单调发布守卫把读模型冻到那条作业结算为止。
  - **读模型不再盖章进程本地指标**：`_refresh_active_task_read_model` 过去把本进程的水位盖到每一行活动 Task 上，两 worker 下就是「谁最后 tick 谁的私有进度冒充全系统」。现在盖的是全库的连续性标记与数据库 lag，并带一道**单调发布守卫**（裁决 PB7）：锁串行化的是写者不是计算，更旧的 tick 整行跳过（不是只跳被盖的字段——混合两轮事实是谁都没观察到的状态），**同一个基准也守清扫**（否则更旧的 tick 删掉更新的行，还会把基准清成 NULL 把守卫解除）。
  - **多 worker 的证明分层**：SQLite 上是确定性交错脚本（完成侧 CAS、`retry` 谓词、水位读——`tests/ansich/test_lease_cas.py`）；SQLite 结构上表达不了的（`skip_locked` 真语义下的认领排他、真正的 advisory 锁排他、READ COMMITTED 丢更新）由 opt-in 的 `tests/integration/test_postgres_multiworker.py` 双 worker tier 在**真 PostgreSQL** 上证明，含 F10-6/F10-20 的**改前红/改后绿**同一交错对照。**执行场所要如实读**：开发者本机、CI 不跑（见下条）。
  - **前端**：Operations 页新增第四个透镜 “Observability”（`AnsichObservabilityHealthPanel`）读 `GET /api/ansich/health`——前端此前从不读它；它与 System 详情抽屉的分界写进两个组件的 docstring（抽屉是**本 worker** 的进程/采集墙，面板是**全库**的答案），两个 failed-job 数**从不合并、从不共用标签**。徽标是**三态**：能连上但有 durably failed 作业是 `attention`，不是绿色的 “reachable”。两个新 `AlertType` 过 exhaustive switch（types/presentation/两份 locale）。
  - **四处 spec 字面偏离（裁决 RB7，已记录）**：§3 的 `ORDER BY job_id` **拒绝**——认领仍按 `(ingest_seq, projector 优先级, projector_name)`，那是文档化的观测内投影优先序，确定性意图已由现有三键满足；§4 的「无 pending 有 failed ⇒ status failed」**拒绝**——失败作业继续读作 `degraded`，采纳字面会让 `failed` 从 A 批 17 边闭包之外的条件可达，而那个闭包的**补集**是合并门禁，其意图由权威的 `database.failed_jobs`、`degraded` 本身与 `projection_failure` 告警三处承载。第三处偏离在 §4 内：projector watermark **读时计算、不落存储列**（便宜到不值得存一份必然漂移的副本）。第四处也在 §4 内：`lag_ms` 取的是「未结算作业里 `MIN(ingest_seq)` 那一行的 `recorded_at` 与现在之差」这个**索引友好式**，而不是 spec §7.2 的字面式——`recorded_at` 上没有索引，按它排序或过滤就是一次全表扫描（`backend/AGENTS.md` 记的就是这一条）。
  - **本批新登记的常驻发现**：F10-29（环境外部化载荷类，三实例）、F10-30（settle-budget flake 家族，终审后共九名成员；P11-C Task 1 补登第十名、Task 2 补登第十一名、Task 11 补登第十二名——第十二名是第一条在目击那一批就完成登记的）、F10-31（LLM attempt 双观测的首写者 pkey 竞态，由双 worker tier 实测）、F10-32（活动 Task 读模型的旧盖章楔子——**它的「没有已部署群体」前提必须在本分支首次部署前重新裁决**）、F10-33（多 worker 下同一 host-Scope episode 碰撞会丢掉整轮 `assess_operations`）、F10-34（P11-C Task 9b 补登：PG tier 的 `_drain_projections` 在退避中的 `retry` 作业前停手,被调用方当成投影已完结——分诊明确**不是** F10-30 也**不是** F10-26,同文件的 `_settle_projections` 就是它的修法）。**该批结束时明确没有清零的残留**：F10-19 的 DOMAIN 残留（活得比自己租约还久的 usage 事务，裁决 PB8 登记不修）、~~F10-6 的 site-1 首写者~~（**已由 P11-C 收口**，`f1d52a79`；而且它当时的严重度评估是错的——那不是「一次可重试的 assessor attempt」，是**整轮** `assess_operations`，见 F10-35）、F10-23 的全扫残留、丢失扫描上界的静默漏报、~~多 worker 下同一 host-Scope episode 的并发碰撞~~（即 F10-33，**已由 P11-C 收口**，`f1d52a79`）。
  - **C 批仍欠（该批结束时的记录；已由下面的 P11-C 条目全部结清）**：§5–§8 的 replay、retention、raw payload 读审计与关闭顺序全部未动；projector join 的时限仍是 C 批前置。
- **Phase 11-C replay、retention、raw read 审计与关闭顺序（2026-08-22..23，`dbfc9c8a`..`89f7a55c` 加本次文档提交）**：Phase 11 的第三批，也是收尾的一批，范围是 [11-resilience-replay-and-retention.md](11-resilience-replay-and-retention.md) 的 **§5–§8**，外加把 A/B 两批留下的继承债一次结清（F10-29/F10-31/F10-32/F10-33/F10-34 与 F10-6 的 tick 侧一半）。**一个迁移 `0028_ansich_retention`**（payload tombstone 三列 + 单行 `ansich_retention_state` + `ansich_active_versions`，外加一个数据步），head 自此是 `0028`。落地的语义：
  - **§5 versioned replay 是一个命令，永远不是一条路由**：`deerflow.ansich.replay`（`plan_replay`/`execute_replay`）加 `python -m deerflow.ansich.replay_cli`。目标集过滤 AND 四个谓词，每一个都挑得有索引可用（kind 清单、`task_id`、`occurred_at` 窗、`ingest_seq` 区间），`recorded_at` 从不读——它没有索引，而 `ansich_observations` 没有 retention，所以那里的全表扫描一天比一天慢。**没有 kind 声明的 projector 不接受时间过滤**（今天恰好只有 `task-spawn-reconcile`），同一处空集也让「按过滤器铸新作业」对它变成错误而不只是无用。写入是 `mint_replay_jobs` 的一个事务：先重新入队、抬 `lease_generation`，再铸新行，再删活动 Task 读模型行——第三步是本批最危险的交互（全局约束 4），而且删除范围**比计划字面更宽**（连续性标记是**全库**的，不是 per-Task），这是记录在案的偏离。
  - **`--replace` 被两次窄化，第二次是本批自己挣来的**：读模型一张表都没有 version 列或指回来源 Observation 的列，所以删除无法像重投影那样被过滤器收窄（裁决 RC4：整表删，且与任何过滤器互斥）。而在真库上一探才发现**独占拥有是必要条件、不是充分条件**——`task-control` 独占 `ansich_transitions`，但 `_project_control` 的 `from_value` 取自共享的 current control Belief，删完再推导会读到已经带着目的地值的 Belief，于是删掉两条只长回来一条，**行数更少且没有一条是历史记下的**。落地为 `_REPLACE_PROVEN_PROJECTORS` 白名单（今天三个），入选是**证明义务**且**两条**都已机械化：可复原（参数化的双 digest 相等 + 全库行数快照不变）与**级联封闭**（owned 集合必须对入向 `ON DELETE CASCADE` 闭合——`task-step` 会顺着一条归属规则看不见的通道删掉兄弟 projector 的七张表，而 SQLite 上 `PRAGMA foreign_keys` 是关的，结构上看不见这一类）。
  - **§5 的 active version 是一行数据库记录，不是配置**（裁决 RC5）：配置字段只能是 startup-only 的，那与「显式的、被审计的管理动作」直接矛盾，还会让一次部署静默改写历史的解释。**没有行就意味着代码默认值**，所以表记的是偏离、空表是正常。`audit_obs_id` 是 `ON DELETE SET NULL`，旁边的 `audit_recorded` 闩把那个 NULL 读成「过期了」而不是「从来没有过」——审计写入本来就允许被降级，没有第二列的话，这两件事在唯一需要分辨的地方就是同一个值。
  - **§6 tier 1：payload tombstone 与三种读者状态**。`ansich_payloads.body` 可空，旁边是 `deleted_at`/`policy`，三列由一个 CHECK 绑在一起动——一个没有 `policy` 戳的 tombstone 能证明「正文被删过」却证明不了「按哪条规则」，那种行写不出来。读者自此分得清**在**／**按策略过期**／**丢失**，只有第三种仍然是一记响亮的抛；raw 路由答 410 Gone，环境 history 给 `expired_points`，认领把已过期的作业按 `expired` 结算并把谱系留在 `last_error` 里（新的第四个状态，裁决 RC7）。tier 1 **两个方向都拒绝孤儿**，这是一道护栏而不是遗漏：rebuild 会删掉 `AnsichAgentReleaseRow` 再在自己的排空里重建它，并发的 retention 会在那个窗口里把被孤立的 manifest 永久 tombstone 掉。
  - **§6 tiers 2/3：跨 pass 收敛，因为按计划字面写会死锁**（控制者裁定的偏离）。每个实体按住自己的发现 Observation、每个 Task 按住自己的创建 Observation（NOT NULL、无 `ON DELETE` 动作、或 `RESTRICT`），所以 tier 2 的连续前缀停在第一条创建 Task 的 Observation 上、horizon 永远是 `0`，而 tier 3 以 horizon 为门禁 ⇒ 两层互等，与三个阈值的相对大小无关。改成按**年龄资格**开闸，tier 2 在**下一趟**走过被解开的前缀：收敛跨 pass，界是依赖图深度，安全性一点没让。删除批次与 horizon 推进在**同一个事务**里（两种拆法各自会在一个方向上说谎），并立了一条新义务：**谁删掉一个 payload 的最后一个引用者，谁就拥有那行 payload**——三个删除者统一走 `_apply_plan_and_reclaim`，它还多伸出一张表去收 `ansich_content_blobs`。
  - **§6 owner/thread 强删除**：`hard_delete_scope` 擦掉一个 owner/thread Scope 与它内部的一切，**优先于时间 retention**（不看任何 cutoff、年龄、tier）。每个 Task 五个阶段，顺序是 schema 逼出来的而不是选出来的；**它从存储而不是从 cursor 恢复**——`within_scope` 边随它的 Task 一起删，所以「还欠哪些 Task」永远等于 Scope 幸存边说的那些，崩溃后重跑同一个调用就把活干完，`0028` 因此没有为它加任何 cursor 行。每一条拒绝都写成「在自己的事务里抛」，好让那条恢复句柄活下来。`POST /api/ansich/retention/hard-delete` 是 admin 路由（它不是 §5 禁的那种「按需跑任意 projector」的端点），**本批刻意不给它前端**。
  - **§7 raw read 审计是本批唯一的 fail-closed 站点**：四条返回 raw body 的路由一律走 admin 闸门 → subject 解析 → **落库的** requested 审计行 → 读 → terminal 审计，访问记录在向存储要正文**之前**提交，写不下就 503 且一个字节都不读。这是全项目 fail-open 规则的一次**记录在案的反转**：别处丢的是关于一次运行的证据，这里证据**就是**那道控制本身。写入是同步的后端写、**从不**走 `record()`（队列按构造无法确认持久化），也**不**走 `persist_and_project`（它的去重是静默跳过，而审计行上的静默跳过就是一个审计缺口）。爆炸半径刻意只有这一族，有测试钉住。
  - **§8 七步有界关停与诚实的启动恢复**：`stop()` 返回一个 `ShutdownReport`，七步各有自己的预算与结果，某一步超时**绝不**中止后面的步骤——否则关停最糟时丢的正是第七步要写下来的证据。第七步不是 `close DB`（engine 归 Gateway、被多个组件共用），而是**进程级丢失桶的排空**，也就是 B 批那句防御性注释预告的第一个真实调用者。预算 `ansich.shutdown_budget_ms` 是**算出来的不是挑出来的**：本序列跑在 lifespan 最外层的 `finally` 里，前面已经过去 50s，而 chart 的 `terminationGracePeriodSeconds` 原本是 45（其注释只算了三项）——本批把默认预算定成 5s、把 grace 提到 60，并把整张串行表写进七处。启动侧：过期 lease 扫描按 `attempts` 分桶且不动 `attempts`/`lease_generation`，active version 校验的三种答案落进健康块加一条 typed WARNING，orphan Run 关联**绝不代宣终态**，producer health **什么都不恢复**（裁决 RC13 按字面落地）。
  - **前端**：Observability 面板补上 retention last run 与 active-version mismatch 三态（`null` = 没读到 / `[]` = 干净 / 列表 = 逐条点名，且**与存储可达性无关**），Scope Effects 视图补上 F10-21 的诚实声明。e2e 抓到一个真 bug：horizon 走的是 `formatAnsichCount`，把一个 ingest 序号渲染成了 `4,211`。
  - **本批新登记的常驻发现**：F10-35（`ansich_current_beliefs` 首写者竞态的**整轮**爆炸半径——移除 F10-31 的类型化容忍后立刻暴露，这正是那条纪律存在的理由）、F10-36（认领处的抛永远变不成一条运维可见的失败作业，全进程投影静默停摆；**既存形状，但本批的 hydrate 提前扩大了它的暴露面**）、F10-37（`_project_control` 不幂等，`task-control` 的普通重放今天就是坏的）、F10-38（owner 擦除的两条 v1 不可解形状，都被如实拒绝且 v1 无产品内补救）、F10-39（`inline_body`／blob 行／tombstone 空壳的无界增长）、F10-40（tier 2 删掉活认领者的观测时，投影侧的外键失败**完全静默**）。全部登记在 [phase-10-review-followups.md](phase-10-review-followups.md)。
  - **明确没有清零的残留**：`run_retention` **今天没有任何调用者**（既不在 tick 里也没有调度入口），所以本批测过的每一种 retention 状态在生产上都还产不出来——这是本批最需要被下一批看见的一句话；F10-36/F10-39/F10-40 三条如上；`--replace` 只对三个已证明的 projector 开放；SQLite 上仍缺一次通用的引用完整性走查（PG arm 是更强的那份证明且它存在，所以这是缺口不是洞）；没有任何结构性装置阻止有人再写一处裸 `rebuild_projections()` + `unsettled == 0`（F10-26 的收尾裁决里点了名）。
- **PostgreSQL 门禁的诚实进度**：新增 opt-in 集成层 `backend/tests/integration/`（`make test-postgres`，由 `DEER_FLOW_TEST_POSTGRES_URL` 开关，未设置时全部自跳，因此 `make test` 仍是纯 SQLite），并**已在真实 PostgreSQL 16.2 上执行通过**：整条 26 个 revision 文件的迁移链（head `0025_ansich_assessor_watermarks`）在空库上 `upgrade head`（`Base.metadata` 的 61 张 `ansich_*` 表全部建出）、`head → 0004_run_ownership → head` 的往返（降级确实删净全部 `ansich_*` 表，再升级复原）、第二次 `upgrade head` 的幂等空转，`bootstrap_schema` 的**三个分支全跑**（含只在 PostgreSQL 上生效的 `pg_advisory_lock` 路径与 legacy 分支的 `0001_baseline` stamp），外加一次端到端服务冒烟（record→flush→projection→Belief→release 质量格→`rebuild_projections()` 复算，`ansich_projection_errors` 为零）。**仍未完成、不得读作已完成**：后端全量测试套件在 PostgreSQL 上的对等运行、多 worker 并发语义（F10-6/F10-20/F10-19 描述的丢更新与丢贡献窗口在 SQLite 单写者下根本不成立，只有多 worker PostgreSQL 能证伪）、关闭 Ansich 的性能基准与生产 paper drill——这三项仍是 Phase 11/12 的门禁。**这段执行记录带日期含义，不要当成「当前链条已过门禁」**：上面那次跑的是 26 个 revision、head 为 `0025_ansich_assessor_watermarks` 的链条；此后环境观测切片追加了 `0026_ansich_environment`，Phase 11-A 批零迁移。
  - **P11-B 的更新（2026-08-21 起，收尾提交与终审修复波在 2026-08-22）**：链条现在是 **29 个 revision 文件、head `0028_ansich_retention`**（该句 2026-08-23 更正：P11-B 结束时是 28 个、head `0027_ansich_lease_generation`，P11-C 的 `0028` 此后成为 head），并且**整条已在真实 PostgreSQL 16.2 上跑过**——迁移矩阵那三条用例断言的是 `_get_head_revision()` 而不是字面量，所以那几轮 `make test-postgres` 覆盖了含 `0026`/`0027` 的完整升级、降级往返与幂等空转。上面三项「仍未完成」里，**多 worker 并发语义那一项部分结清**：新增的双 worker tier（`tests/integration/test_postgres_multiworker.py`）在真 PG 上证明了认领排他（真 `skip_locked`）、租约接管后的完成 CAS、advisory 锁的真实排他与失败后释放、F10-6/F10-20 丢更新的**改前红改后绿**同一交错对照、锁序与死锁，以及跨 worker 一致的健康计数；**F10-19 的丢贡献窗口只被收窄、没有关闭**（DOMAIN 残留见该条）。**仍未完成**：后端全量测试套件在 PostgreSQL 上的对等运行、关闭 Ansich 的性能基准与生产 paper drill。**执行场所照旧要如实读**：这一层由开发者本机执行，CI 从不设 `DEER_FLOW_TEST_POSTGRES_URL`，因此 CI 跑的是自跳；P11-B 的验收跑在 `pgserver` wheel `initdb` 出来的一次性本地 PostgreSQL 16.2 集群上（该环境 Docker 守护进程不可用），连跑五轮各 19 passed。
  - **P11-C 的更新（2026-08-22..23）**：双 worker tier 从 **19** 条长到 **28** 条，全部在同一份 `pgserver` PostgreSQL 16.2 上跑过；新增的那九条打的都是 SQLite 结构上表达不了的地方，其中几处值得点名：replay 与 `--replace` 在 asyncpg 方言上的 digest 与两次一致、第二个 worker 在 mint 事务打开期间**提交**自己的 `assess_operations`、retention tier 2 删掉一个**活认领者**的作业、hard delete 与活认领者并排、关停与活对端并排、以及**反序取 Belief 行的死锁 arm**（交叉序恰好一次中止，同序两边都过——F1 那一类必须在服务器上证明，SQLite 死锁不了）。迁移矩阵那三条用例断言的是 `_get_head_revision()` 而不是字面量，所以含 `0028` 的完整升级／降级往返／幂等空转也随之覆盖。**一处执行缺口如实记下**：Task 11 的复审期间 5433 上的服务器中途掉了，那一轮的 26 arm 是靠读代码审的、没有复跑；下一个任务（T12）重启服务器并在自己的 HEAD 上跑通了 26，缺口就此补上。**上面三项「仍未完成」里剩下的两项不变**：后端全量测试套件在 PostgreSQL 上的对等运行、关闭 Ansich 的性能基准与生产 paper drill。执行场所照旧要如实读：这一层由开发者本机执行，CI 从不设 `DEER_FLOW_TEST_POSTGRES_URL`。
- Phase 1 代码评审的跟进项登记在 [phase-1-review-followups.md](phase-1-review-followups.md)，各项带有修复状态、归属阶段与对应 commit；Phase 2 的实施前提 F4（projector 显式优先级）已修复。
- Phase 2 代码评审的跟进项登记在 [phase-2-review-followups.md](phase-2-review-followups.md)；H2（上下文快照写放大与谱系完整性）的剩余压缩 inventory 与 derived-from 边已随 Phase 4 commit `a596a310` 落地，现已完成。
- Phase 3 代码评审的跟进项登记在 [phase-3-review-followups.md](phase-3-review-followups.md)；M2（transform 显式元数据）与 M3（队列字节水位）均已完成，Phase 5 前置项已清；L1 观察开销基准按既定归属留到 Phase 12 验收前，不阻塞 Phase 5。
- Phase 4 代码评审的跟进项登记在 [phase-4-review-followups.md](phase-4-review-followups.md)；M1/M2/M3、L1/L2 与 L3①/② 均已完成；L3② 的前端压缩列表独立 API 已作为 Phase 6 首批 UI/API 工作落地。
- Phase 5 代码评审的跟进项登记在 [phase-5-review-followups.md](phase-5-review-followups.md)；M1/M2/M3 与 L1/L2 均已修复，L3（usage 贡献契约收紧）也已由 `fe4634bf` 修复，Phase 8 前的 Phase 5 到期项已清。
- Phase 6 代码评审的跟进项登记在 [phase-6-review-followups.md](phase-6-review-followups.md)；L3（wall_time 覆盖语义）已由 `b910ba82` 修复，M1（assessor job 合并）与 L1（对账读放大）已由 `4f5ec989` 修复；L4 已由 `0a38a96d` 明确延后到 Phase 11 并隐藏未生产类型。Phase 7 开工前置项已清；L2（孤儿 requested action 回收）已由 `a1bcf523` 在 Phase 11 前的加固批中修复（5 分钟保守过期 + 请求驱动的 CAS 接管，被放弃尝试的终态留在不可变 Observation 流里），该文件已无到期项。
- Phase 7 代码评审的跟进项登记在 [phase-7-review-followups.md](phase-7-review-followups.md)；M2（coalescing 测试套件级 flaky）已由 `e91d9f1c`/`4e5eb0fd` 修复并隔离 SQL 集成测试 settle 时序，M1（drift 裸名判定）已由 `08cd6b1c` 修复，M3（policy manifest 契约化）已由 `256d2c91` 修复，L2（descriptor 私有通道回归）已随 Phase 8 的显式 assembly/context 通道由 `79cd13b1` 完成；L1（二线 credential validator）已于 Phase 9 开工前随本次提交完成。
- Phase 8 代码评审的跟进项登记在 [phase-8-review-followups.md](phase-8-review-followups.md)；M1（wall_time 双写者）已由 `459c170e` 修复——`_refresh_usage_summary` 成为 wall_time summary 的唯一写者；M2（heartbeat contribution 写/读放大）已由 `7c8edb26`/`2443fc99` 按方向 ① 修复——wall_time 改为每 `(aggregate, source)` 一行的 max 型高水位通道，历史 per-tick 行由迁移 `0024_ansich_wall_time_watermarks` 折叠，方向 ②（sum 维度的通用增量刷新）按裁决 HR1 留到 Phase 11；L1（吞 CancelledError）与 L2（tree N+1）随相关路径下次改动顺带处理。
- 非评审来源的人工发现跟进项登记在 [human-followups.md](human-followups.md)；各项附施工时机评估，每个 Phase 开工前复查该文件。已结清：D1（概念词汇表，`152f44c2`，并随实现扩写至 Phase 10）、U3（failed-jobs 下钻，`5b5d03a7`）、D2 的内联 tooltip 层（`3425a7a1`）。仍欠：U4（compression 执行时机，已过其建议窗口，若拖到 U1 开工则并入 U1）、**U1（Lineage UI 重构）与 A1（obs layer 抽象）现已到期**——两者的归属都是「Phase 10 完成后」，而 Phase 10 已完成；U2 与 A1 的实际迁移归 Phase 11，D2 的正式使用文档归 Phase 12 §9。
- Phase 9 代码评审的跟进项登记在 [phase-9-review-followups.md](phase-9-review-followups.md)；H1–H7 均已修复，其中 H6 让 scope-safety assessor 在 subject ToolCall Entity 尚未投影时无 attempt 等待并可自愈（等待带持久化 `dependency_pending_since` 锚点并复用 `projector_dependency_timeout_seconds` 上界，subject 永不出现时越界转 durable failed 进入 failed-job 重试诊断面，迁移 `0022_ansich_assessor_deadline`），H7 在保留 Collector fail-open 语义的同时补齐 queue count/byte overflow 的限速结构化持久日志，并校正了既有 `observability.degraded`/active read model 部分持久化路径的边界。M1（effect class 分类法缺 filesystem_delete/permission_change）已由 `faea4bc6`/`018d5b1b` 修复——bash 首命令词在单一简单命令下分类为 `filesystem_delete`/`permission_change`，observed 侧只记 `inferred` fidelity 且每次调用保留 `unknown` 伴随行，因此不改变 `unverified_effect` 结论；M2（scope-safety assessor 全量重扫）已由 `fda506de`/`89217ae5` 修复——只重估新 watermark 区间点名的 tool_call，区间下界落在持久表 `ansich_assessor_watermarks`（迁移 `0025_ansich_assessor_watermarks`）并在 claim 事务内下调；L1/L2 仍为非阻塞清理项。
- Phase 10 代码评审的跟进项登记在 [phase-10-review-followups.md](phase-10-review-followups.md)；终审的三个合并门禁项 F10-1（`alert_subject_is_not_a_task` 降级分支回归）、F10-2（compare 参数顺序 e2e 钉子）、F10-3（release 聚合格 cohort 取值偏离入档）已随终审修复，其余项均带修复方向与归属：F10-4（按 cohort 取样）、F10-5（`score_count` 分母）、F10-6（兄弟 rollup 串行化）归 Phase 11，F10-7（回执可能永远 pending）已由 P11-A 批的 `6a70072a`/`6665611f` 结清（改为按 observation 状态解析的四级阶梯），F10-8（`_validate_subject` payload-None 守卫）已由 `d463a604` 修复（assessor 侧兄弟仍开放，见 F10-23）、F10-10（settle 时序 flaky 隔离）已由 `04f7ce96`/`25168118` 修复（门禁只被 Task 8 的验收负载证明过，更重负载下的充分性边界记在 `e53cefbc`，成因假说 (c) 见该条留观记录），F10-9（六处常量收敛）随下次相关改动，F10-16/F10-17 归 UI-2 与后端 release 级聚合落地时，其余为非阻塞清理项。
- 回溯验证矩阵登记在 [retro-validation-matrix.md](retro-validation-matrix.md)：六个故障注入用例分别对应 upstream 已关闭 issue（#3320/#4041、#3684、#4176、#3875、#3113、#3645），检验 Observation→Belief 是否足以独立判定这六类错误。预测在跑测试**之前**已预注册，结果表记录实际判定与差异。结论：零失败项，一笔弱通过（#3645 缺 by-model 聚合查询，证据齐全——已由 Phase 11 前加固批的 `f697ac7b` 补上 `GET /tasks/{id}/usage?by=model` 结清），一处能力边界确认（bash 无 instrumentation，`coverage="unknown"` 属设计使然）。测试位于 `backend/tests/ansich/test_retro_*.py`，共享 harness 在 `backend/tests/support/ansich_retro.py`。

## 固定实现边界

以下决策适用于全部阶段，若实现中需要改变，必须先修订设计文档和受影响的阶段计划：

1. `backend/packages/ansich/` 是独立 Python 包，放置 Observation、Entity、Belief、Relation 等领域契约，以及不依赖 DeerFlow、LangGraph、FastAPI 的投影和查询逻辑。
2. `backend/packages/harness/deerflow/ansich/` 是 DeerFlow 适配层，放置 SQLAlchemy 表、仓储、Gateway 进程内服务装配和运行时探针。依赖方向只能是 `deerflow -> ansich`。
3. `backend/app/gateway/routers/ansich.py` 只做认证、请求校验、DTO 转换和调用应用服务，不在路由中写投影或规则逻辑。
4. `frontend/src/core/ansich/` 负责 API 类型、TanStack Query hooks 和纯转换函数；`frontend/src/app/workspace/ansich/` 与 `frontend/src/components/workspace/ansich/` 负责页面和组件。
5. Ansich 与 DeerFlow 共用已配置的 SQLAlchemy Engine/SessionFactory，但只写 `ansich_*` 表，不修改 `run_events` 语义。
6. 所有采集失败均 fail-open；现有 Token、循环、授权、超时等 DeerFlow 执行保护仍在本地同步路径中 fail-safe。
7. 除 Observation/Payload 外，Entity、Belief、Relation、Usage 和 Read Model 都是可删除、可按 projector version 重放的投影。
8. 后端实现强制 TDD。每个阶段先提交失败测试，再实现；SQLite 和 PostgreSQL 语义都要覆盖。前端新增行为必须有 unit test，关键操作流必须有 Playwright E2E。

## 阶段索引

1. [第一条可观测 Task](01-first-observable-task.md)
2. [逻辑 Step 与实际模型输入](02-logical-steps-and-model-inputs.md)
3. [ToolCall 责任链](03-toolcall-accountability.md)
4. [上下文谱系与压缩](04-context-lineage-and-compression.md)
5. [活动任务、心跳与预算](05-active-tasks-heartbeats-and-budgets.md)
6. [失控告警与干预](06-runaway-alerts-and-intervention.md)
7. [AgentRelease 与版本比较](07-agent-release-and-comparison.md)
8. [子 Agent Task 树与 inclusive usage](08-subagent-task-tree-and-inclusive-usage.md)
9. [Scope、授权与副作用审计](09-scope-authorization-and-effects.md)
10. [评估输入与语义 Belief](10-evaluation-and-semantic-beliefs.md)
11. [生产韧性、重放与保留策略](11-resilience-replay-and-retention.md)
12. [验收演练与生产就绪](12-acceptance-drills-and-production-readiness.md)

## 阶段合并规则

每个阶段完成时必须同时满足：

- 迁移可在空 SQLite、已有 SQLite、空 PostgreSQL、已有 PostgreSQL 上执行；降级策略有明确说明。
- 新增 Observation kind 有 schema version、幂等 source event ID 生成规则和至少一个重复/乱序测试。
- 新增状态字段返回完整 Belief 结构，不允许 API 暴露裸状态枚举。
- UI 显示投影时间、丢失范围或 `unknown`/`unassessed`，不把资料缺失展示成健康或零值。
- 失败注入证明 Ansich 不改变 DeerFlow Run 的业务结果。
- 相关 README、`backend/AGENTS.md`、`frontend/AGENTS.md` 与配置示例同步更新。
- 新增或改变领域概念时同步更新 [../concepts.md](../concepts.md)（human-followups D1 的配套动作，防止概念导览再次腐化）。
